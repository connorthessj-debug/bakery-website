

I'll implement AI features for the Le Besian Balls bakery website. For a bakery, AI features should enhance the customer experience without being gimmicky. I'll build:

1. **AI Bakery Assistant ChatWidget** - Helps customers with product questions, allergen info, ordering guidance
2. **AI-powered Product Recommendations** - Suggests products based on preferences
3. **Smart Search** - AI-enhanced search with natural language understanding
4. **AI Configuration & Utilities** - Centralized config with cost controls and fallbacks

Let me build each component:

<tool_call>
{"name": "write_code", "arguments": {"filename": "src/lib/ai-config.ts", "code": "/**\n * AI Configuration for Le Besian Balls Bakery Website\n * \n * Centralized configuration for all AI features.\n * Includes rate limiting, cost controls, fallback strategies,\n * and prompt versioning.\n * \n * @version 1.0.0\n * @author Sage - AI/ML Specialist\n */\n\n// ============================================\n// ENVIRONMENT & API CONFIGURATION\n// ============================================\n\nexport const AI_CONFIG = {\n  // API Settings\n  provider: 'openai' as const,\n  model: process.env.NEXT_PUBLIC_AI_MODEL || 'gpt-3.5-turbo',\n  apiKey: process.env.OPENAI_API_KEY || '',\n  \n  // Cost Controls\n  maxTokensPerRequest: 500,\n  maxTokensPerDay: 50000,\n  estimatedCostPerRequest: 0.002, // ~$0.002 per request with gpt-3.5-turbo\n  monthlyBudgetCap: 50, // $50/month cap\n  \n  // Rate Limiting\n  rateLimits: {\n    requestsPerMinute: 10,\n    requestsPerHour: 100,\n    requestsPerDay: 1000,\n  },\n  \n  // Timeouts\n  requestTimeout: 10000, // 10 seconds\n  retryAttempts: 2,\n  retryDelay: 1000, // 1 second\n  \n  // Feature Flags\n  features: {\n    chatWidget: process.env.NEXT_PUBLIC_AI_CHAT_ENABLED === 'true',\n    productRecommendations: process.env.NEXT_PUBLIC_AI_RECOMMENDATIONS_ENABLED === 'true',\n    smartSearch: process.env.NEXT_PUBLIC_AI_SEARCH_ENABLED === 'true',\n  },\n} as const;\n\n// ============================================\n// SYSTEM PROMPTS (Versioned)\n// ============================================\n\nexport const SYSTEM_PROMPTS = {\n  chatAssistant: {\n    version: '1.0.0',\n    prompt: `You are the friendly AI assistant for \"Le Besian Balls\" bakery. Your name is Besi.\n\nYour personality:\n- Warm, helpful, and enthusiastic about baked goods\n- Knowledgeable about ingredients, allergens, and baking\n- You speak in a friendly, conversational tone\n- You occasionally use bakery-related puns (sparingly!)\n\nYour capabilities:\n- Answer questions about our products, ingredients, and allergens\n- Help customers choose products based on their preferences\n- Provide information about ordering, delivery, and store hours\n- Suggest products for special occasions\n\nImportant rules:\n- NEVER make up specific prices — say \"please check our menu for current pricing\"\n- NEVER guarantee specific delivery times — say \"typically\" or \"usually\"\n- If asked about something you don't know, say \"I'd recommend contacting us directly for that!\"\n- Keep responses concise (2-3 sentences max unless more detail is requested)\n- If someone has allergy concerns, ALWAYS recommend they confirm with staff directly\n- Do NOT provide medical or nutritional advice\n\nStore Information:\n- We're an artisan bakery specializing in handcrafted breads, pastries, and cakes\n- Everything is baked fresh daily\n- We accommodate common dietary needs (gluten-free options, vegan options available)\n- Custom orders are available with advance notice`,\n  },\n\n  productRecommendation: {\n    version: '1.0.0',\n    prompt: `You are a product recommendation engine for \"Le Besian Balls\" bakery.\n\nGiven a customer's preferences or occasion, suggest 2-3 products from our catalog.\n\nRespond ONLY in valid JSON format:\n{\n  \"recommendations\": [\n    {\n      \"category\": \"string\",\n      \"suggestion\": \"string\",\n      \"reason\": \"string (one sentence)\"\n    }\n  ],\n  \"message\": \"string (friendly one-liner)\"\n}\n\nCategories: breads, pastries, cakes, cookies, seasonal\n\nKeep reasons short and appetizing. Never invent specific product names not provided in context.`,\n  },\n\n  smartSearch: {\n    version: '1.0.0',\n    prompt: `You are a search query interpreter for \"Le Besian Balls\" bakery website.\n\nGiven a natural language search query, extract the intent and return structured search parameters.\n\nRespond ONLY in valid JSON format:\n{\n  \"intent\": \"product_search\" | \"info_query\" | \"order_query\",\n  \"categories\": [\"string\"],\n  \"dietary\": [\"string\"] | null,\n  \"occasion\": \"string\" | null,\n  \"keywords\": [\"string\"],\n  \"suggestion\": \"string (friendly search suggestion to display)\"\n}\n\nDietary options: gluten-free, vegan, nut-free, dairy-free\nCategories: breads, pastries, cakes, cookies, seasonal`,\n  },\n} as const;\n\n// ============================================\n// FALLBACK RESPONSES\n// ============================================\n\nexport const FALLBACK_RESPONSES = {\n  chatOffline: \"I'm taking a quick break from the oven! 🍞 Please browse our menu or contact us directly for help.\",\n  chatError: \"Oops, I got a little flour in my circuits! Please try again in a moment, or feel free to call us directly.\",\n  chatRateLimit: \"I'm helping lots of customers right now! Please try again in a minute. 🧁\",\n  \n  recommendationsFallback: [\n    {\n      category: 'pastries',\n      suggestion: 'Our freshly baked croissants',\n      reason: 'A customer favorite — buttery, flaky, and perfect any time of day.',\n    },\n    {\n      category: 'breads',\n      suggestion: 'Artisan sourdough loaf',\n      reason: 'Made with our signature 24-hour fermented starter.',\n    },\n    {\n      category: 'cakes',\n      suggestion: 'Seasonal specialty cake',\n      reason: 'Ask about our rotating seasonal flavors!',\n    },\n  ],\n\n  searchFallback: {\n    intent: 'product_search' as const,\n    categories: [] as string[],\n    dietary: null,\n    occasion: null,\n    keywords: [] as string[],\n    suggestion: 'Browse our full menu to find something delicious!',\n  },\n} as const;\n\n// ============================================\n// QUICK REPLIES FOR CHAT\n// ============================================\n\nexport const QUICK_REPLIES = [\n  { label: '📋 View Menu', action: 'show_menu', response: 'You can browse our full menu on the Products page! We have fresh breads, pastries, cakes, and cookies baked daily.' },\n  { label: '🕐 Store Hours', action: 'store_hours', response: 'We\\'re open Monday–Friday 7am–6pm, Saturday 8am–5pm, and Sunday 8am–2pm. We recommend coming early for the best selection!' },\n  { label: '🎂 Custom Orders', action: 'custom_orders', response: 'We love custom orders! For custom cakes and large orders, please contact us at least 48 hours in advance. You can reach us through the Contact page or call us directly.' },\n  { label: '🌾 Allergen Info', action: 'allergen_info', response: 'We take allergies seriously! We offer gluten-free and vegan options. However, please note our kitchen handles wheat, nuts, dairy, and eggs. Always confirm specific allergen concerns with our staff.' },\n  { label: '🚗 Delivery', action: 'delivery', response: 'We offer local delivery for orders over a minimum amount. Delivery times vary — check our ordering page or contact us for current delivery availability in your area!' },\n] as const;\n\n// ============================================\n// TYPE DEFINITIONS\n// ============================================\n\nexport interface ChatMessage {\n  id: string;\n  role: 'user' | 'assistant' | 'system';\n  content: string;\n  timestamp: Date;\n  isError?: boolean;\n  isFallback?: boolean;\n}\n\nexport interface RecommendationResult {\n  recommendations: Array<{\n    category: string;\n    suggestion: string;\n    reason: string;\n  }>;\n  message: string;\n  isFallback?: boolean;\n}\n\nexport interface SearchResult {\n  intent: 'product_search' | 'info_query' | 'order_query';\n  categories: string[];\n  dietary: string[] | null;\n  occasion: string | null;\n  keywords: string[];\n  suggestion: string;\n  isFallback?: boolean;\n}\n\nexport type AIFeatureStatus = 'ready' | 'loading' | 'error' | 'disabled' | 'rate_limited';\n"}}
</tool_call>

<tool_call>
{"name": "write_code", "arguments": {"filename": "src/lib/ai-client.ts", "code": "/**\n * AI Client — Handles all communication with AI services\n * \n * Features:\n * - Rate limiting with token bucket algorithm\n * - Automatic retries with exponential backoff\n * - Cost tracking\n * - Graceful fallbacks when AI is unavailable\n * - Request/response logging for debugging\n * \n * @version 1.0.0\n * @author Sage - AI/ML Specialist\n */\n\nimport {\n  AI_CONFIG,\n  SYSTEM_PROMPTS,\n  FALLBACK_RESPONSES,\n  type ChatMessage,\n  type RecommendationResult,\n  type SearchResult,\n} from './ai-config';\n\n// ============================================\n// RATE LIMITER\n// ============================================\n\nclass RateLimiter {\n  private requests: number[] = [];\n  private readonly windowMs: number;\n  private readonly maxRequests: number;\n\n  constructor(maxRequests: number, windowMs: number) {\n    this.maxRequests = maxRequests;\n    this.windowMs = windowMs;\n  }\n\n  canMakeRequest(): boolean {\n    const now = Date.now();\n    this.requests = this.requests.filter((time) => now - time < this.windowMs);\n    return this.requests.length < this.maxRequests;\n  }\n\n  recordRequest(): void {\n    this.requests.push(Date.now());\n  }\n\n  getTimeUntilAvailable(): number {\n    if (this.canMakeRequest()) return 0;\n    const oldest = this.requests[0];\n    return oldest + this.windowMs - Date.now();\n  }\n}\n\n// Per-minute rate limiter\nconst rateLimiter = new RateLimiter(\n  AI_CONFIG.rateLimits.requestsPerMinute,\n  60 * 1000\n);\n\n// ============================================\n// COST TRACKER\n// ============================================\n\nclass CostTracker {\n  private dailyRequests = 0;\n  private dailyCost = 0;\n  private lastReset: Date = new Date();\n\n  private resetIfNewDay(): void {\n    const now = new Date();\n    if (now.toDateString() !== this.lastReset.toDateString()) {\n      this.dailyRequests = 0;\n      this.dailyCost = 0;\n      this.lastReset = now;\n    }\n  }\n\n  canAffordRequest(): boolean {\n    this.resetIfNewDay();\n    const projectedDailyCost = this.dailyCost + AI_CONFIG.estimatedCostPerRequest;\n    const dailyBudget = AI_CONFIG.monthlyBudgetCap / 30;\n    return projectedDailyCost < dailyBudget;\n  }\n\n  recordRequest(estimatedTokens: number = AI_CONFIG.maxTokensPerRequest): void {\n    this.resetIfNewDay();\n    this.dailyRequests++;\n    // Rough cost estimate: $0.002 per 1K tokens for gpt-3.5-turbo\n    this.dailyCost += (estimatedTokens / 1000) * 0.002;\n  }\n\n  getStats() {\n    this.resetIfNewDay();\n    return {\n      dailyRequests: this.dailyRequests,\n      estimatedDailyCost: this.dailyCost.toFixed(4),\n      dailyBudget: (AI_CONFIG.monthlyBudgetCap / 30).toFixed(2),\n    };\n  }\n}\n\nconst costTracker = new CostTracker();\n\n// ============================================\n// CORE AI CLIENT\n// ============================================\n\ninterface AIRequestOptions {\n  systemPrompt: string;\n  userMessage: string;\n  maxTokens?: number;\n  temperature?: number;\n}\n\nasync function makeAIRequest(options: AIRequestOptions): Promise<string> {\n  const { systemPrompt, userMessage, maxTokens = AI_CONFIG.maxTokensPerRequest, temperature = 0.7 } = options;\n\n  // Check rate limits\n  if (!rateLimiter.canMakeRequest()) {\n    throw new AIError('RATE_LIMITED', 'Too many requests. Please wait a moment.');\n  }\n\n  // Check cost budget\n  if (!costTracker.canAffordRequest()) {\n    throw new AIError('BUDGET_EXCEEDED', 'Daily AI budget reached.');\n  }\n\n  // Check API key\n  if (!AI_CONFIG.apiKey) {\n    throw new AIError('NO_API_KEY', 'AI service not configured.');\n  }\n\n  rateLimiter.recordRequest();\n\n  const controller = new AbortController();\n  const timeoutId = setTimeout(() => controller.abort(), AI_CONFIG.requestTimeout);\n\n  try {\n    const response = await fetch('https://api.openai.com/v1/chat/completions', {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n        Authorization: `Bearer ${AI_CONFIG.apiKey}`,\n      },\n      body: JSON.stringify({\n        model: AI_CONFIG.model,\n        messages: [\n          { role: 'system', content: systemPrompt },\n          { role: 'user', content: userMessage },\n        ],\n        max_tokens: maxTokens,\n        temperature,\n      }),\n      signal: controller.signal,\n    });\n\n    clearTimeout(timeoutId);\n\n    if (!response.ok) {\n      if (response.status === 429) {\n        throw new AIError('RATE_LIMITED', 'AI service rate limited.');\n      }\n      if (response.status === 401) {\n        throw new AIError('AUTH_ERROR', 'AI